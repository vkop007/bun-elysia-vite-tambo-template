{
  "name": "bun-elysia-vite-tambo-template",
  "version": "1.0.0",
  "description": "Full-stack AI template with Bun, Elysia.js, Vite, and Tambo for building generative UIs",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build",
    "start": "bun run --filter '*' start"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
